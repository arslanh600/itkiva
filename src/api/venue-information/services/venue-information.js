'use strict';

/**
 * venue-information service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::venue-information.venue-information');
